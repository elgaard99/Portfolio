@page "/skills"
@inject YamlLocalizationService LocalizationService
<PageTitle>@LocalizationService["PageTitle"]</PageTitle>


<div class="container-fluid">
  <div class="row">
    <!-- Empty column on the left -->
    <div class="col-md-2"></div>

    <!-- Main content column -->
    <div class="col-md-8">
      <div class="py-5">
        <h1 class="text-center fw-bold mb-5">@LocalizationService["Header"] 🧰</h1>
            
        <!-- Full-width card for TechStack_Body with pill-style tech tags -->
        <div class="row mb-4">
          <div class="col-12">
            <div class="card shadow-sm border-0">
              <div class="card-body text-start">
                <h4 class="">🛠️ Tech Stack</h4>
                <p class="mb-3">@LocalizationService["TechStack_Body"]</p>
                <div class="d-flex flex-wrap gap-2">
                  <span class="badge rounded-pill bg-light text-dark border">C#</span>
                  <span class="badge rounded-pill bg-light text-dark border">.NET</span>
                  <span class="badge rounded-pill bg-light text-dark border">Blazor</span>
                  <span class="badge rounded-pill bg-light text-dark border">SQL</span>
                  <span class="badge rounded-pill bg-light text-dark border">HTML / CSS</span>
                  <span class="badge rounded-pill bg-light text-dark border">Git</span>
                  <span class="badge rounded-pill bg-light text-dark border">Visual Studio</span>
                  <span class="badge rounded-pill bg-light text-dark border">ChatGPT</span>
                  <span class="badge rounded-pill bg-light text-dark border">Vault</span>
                  <span class="badge rounded-pill bg-light text-dark border">K8s</span>
                  <span class="badge rounded-pill bg-light text-dark border">Prometheus</span>
                  <span class="badge rounded-pill bg-light text-dark border">Grafana</span>
                  <span class="badge rounded-pill bg-light text-dark border">WPF</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="row g-4">
          <!-- Frontend Development -->
          <div class="col-md-6 col-lg-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h4 class="card-title text-dark fw-bold mb-3">🖥️ Frontend Development</h4>
                <ul class="list-unstyled mb-0">
                  <li><strong>@LocalizationService["HtmlLabel"]</strong> @LocalizationService["Html"]</li>
                  <li><strong>@LocalizationService["BlazorLabel"]</strong> @LocalizationService["Blazor"]</li>
                  <li><strong>@LocalizationService["UIUXLabel"]</strong> @LocalizationService["UI"]</li>
                  <li><strong>@LocalizationService["PythonLabel"]</strong> @LocalizationService["Python"]</li>
                </ul>
              </div>
            </div>
          </div>

          <!-- Backend Development -->
          <div class="col-md-6 col-lg-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h4 class="card-title text-dark fw-bold mb-3">⚙️ Backend Development</h4>
                <ul class="list-unstyled mb-0">
                  <li><strong>@LocalizationService["SqlTsqlLabel"]</strong> @LocalizationService["Sql"]</li>
                  <li><strong>@LocalizationService["CsharpNetLabel"]</strong> @LocalizationService["Csharp"]</li>
                  <li><strong>@LocalizationService["RedisLabel"]</strong> @LocalizationService["Redis"]</li>
                  <li><strong>@LocalizationService["UMLLabel"]</strong> @LocalizationService["UML"]</li>
                </ul>
              </div>
            </div>
          </div>

          <!-- Software Development -->
          <div class="col-md-6 col-lg-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h4 class="card-title text-dark fw-bold mb-3">🎯 Software Development</h4>
                <ul class="list-unstyled mb-0">
                  <li><strong>@LocalizationService["ArchitectureLabel"]</strong> @LocalizationService["Architecture"]</li>
                  <li><strong>@LocalizationService["VersioningLabel"]</strong> @LocalizationService["Versioning"]</li>
                  <li><strong>@LocalizationService["ProjectManagementLabel"]</strong> @LocalizationService["ProjectManagement"]</li>
                  <li><strong>@LocalizationService["ProccessModelsLabel"]</strong> @LocalizationService["ProccessModels"]</li>
                </ul>
              </div>
            </div>
          </div>

          <!-- DevOps -->
          <div class="col-md-6 col-lg-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h4 class="card-title text-dark fw-bold mb-3">🏗️ DevOps</h4>
                <ul class="list-unstyled mb-0">
                  <li><strong>@LocalizationService["CiCdLabel"]</strong> @LocalizationService["CiCd"]</li>
                  <li><strong>@LocalizationService["ItSecurityLabel"]</strong> @LocalizationService["ItSecurity"]</li>
                  <li><strong>@LocalizationService["MonitoringLabel"]</strong> @LocalizationService["Monitoring"]</li>
                  <li><strong>@LocalizationService["LinuxLabel"]</strong> @LocalizationService["Linux"]</li>
                </ul>
              </div>
            </div>
          </div>

          <!-- Cloud -->
          <div class="col-md-6 col-lg-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h4 class="card-title text-dark fw-bold mb-3">☁️ Cloud</h4>
                <ul class="list-unstyled mb-0">
                  <li><strong>@LocalizationService["AzureLabel"]</strong> @LocalizationService["Azure"]</li>
                  <li><strong>@LocalizationService["IaCLabel"]</strong> @LocalizationService["IaC"]</li>
                  <li><strong>@LocalizationService["KubernetesLabel"]</strong> @LocalizationService["Kubernetes"]</li>
                  <li><strong>@LocalizationService["CloudSecurityLabel"]</strong> @LocalizationService["CloudSecurity"]</li>
                </ul>
              </div>
            </div>
          </div>

          <!-- Værktøjer -->
          <div class="col-md-6 col-lg-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h4 class="card-title text-dark fw-bold mb-3">🧰 @LocalizationService["Tools_Header"]</h4>
                <ul class="list-unstyled mb-0">
                  <li><strong>@LocalizationService["IDEsLabel"]</strong> @LocalizationService["IDEs"]</li>
                  <li><strong>@LocalizationService["AiLabel"]</strong> @LocalizationService["Ai"]</li>
                  <li><strong>@LocalizationService["FedoraWindowsLabel"]</strong> @LocalizationService["FedoraWindows"]</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </div>
      <!-- Empty column on the right -->
      <div class="col-md-2"></div>
    </div>
  </div>
</div>




@code {
    protected override void OnInitialized()
    {
        LocalizationService.LanguageChanged += OnLanguageChanged;
    }

    private void OnLanguageChanged()
    {
        InvokeAsync(StateHasChanged); // refresh UI
    }

    protected override async Task OnParametersSetAsync()
    {
        await LocalizationService.LoadTranslations("Skills");

        await base.OnParametersSetAsync();
    }

    public void Dispose()
    {
        LocalizationService.LanguageChanged -= OnLanguageChanged;
    }
}